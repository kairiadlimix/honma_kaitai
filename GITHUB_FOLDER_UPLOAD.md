# GitHub WebUIでフォルダ構造をアップロードする方法

## 🎯 最も簡単な方法：フォルダをドラッグ&ドロップ

### ステップ1: フォルダを準備

```bash
# プロジェクトルートに移動
cd /Users/kairi.oshima/honma_kaitai

# 不要なファイルを除外（重要！）
# node_modulesは除外する必要があります
```

### ステップ2: Finderでフォルダを開く

1. Finderを開く
2. `dashboard` フォルダに移動
3. **フォルダ全体を選択**（フォルダ名をクリック）

### ステップ3: GitHubにドラッグ&ドロップ

1. GitHubのリポジトリページで「upload files」をクリック
2. Finderから `dashboard` フォルダをドラッグ
3. GitHubのアップロード画面にドロップ
4. **フォルダ構造が自動的に保持されます**

---

## 📂 実際の例

### 現在のフォルダ構造

```
dashboard/
├── app/
│   ├── (dashboard)/
│   │   ├── page.tsx
│   │   ├── machines/
│   │   │   └── page.tsx
│   │   └── rankings/
│   │       └── page.tsx
│   └── api/
│       └── dashboard/
│           └── summary/
│               └── route.ts
├── components/
│   ├── dashboard/
│   │   └── kpi-card.tsx
│   └── layout/
│       ├── header.tsx
│       └── sidebar.tsx
├── lib/
│   ├── mock-data.ts
│   └── utils.ts
└── package.json
```

### ドラッグ&ドロップ後のGitHub上の構造

**同じ構造がそのまま保持されます**：

```
dashboard/
├── app/
│   ├── (dashboard)/
│   │   ├── page.tsx
│   │   ├── machines/
│   │   │   └── page.tsx
│   │   └── rankings/
│   │       └── page.tsx
│   └── api/
│       └── dashboard/
│           └── summary/
│               └── route.ts
├── components/
│   ├── dashboard/
│   │   └── kpi-card.tsx
│   └── layout/
│       ├── header.tsx
│       └── sidebar.tsx
├── lib/
│   ├── mock-data.ts
│   └── utils.ts
└── package.json
```

---

## ⚠️ 重要な注意事項

### node_modulesを除外する方法

**方法1: アップロード前に除外**

```bash
# 一時的にnode_modulesを別名に変更
cd /Users/kairi.oshima/honma_kaitai/dashboard
mv node_modules node_modules_backup

# アップロード後、必要に応じて戻す
# mv node_modules_backup node_modules
```

**方法2: .gitignoreを設定（推奨）**

`.gitignore`ファイルがあれば、GitHubが自動的に除外しますが、WebUIでは`.gitignore`が効かない場合があります。

**方法3: 手動で除外**

1. Finderで `dashboard` フォルダを開く
2. `node_modules` フォルダを削除（または別の場所に移動）
3. 残りのフォルダをドラッグ&ドロップ

### 除外すべきフォルダ/ファイル

アップロード前に以下を除外：

- ❌ `node_modules/` - 大容量、不要
- ❌ `.next/` - ビルド成果物
- ❌ `.env.local` - 機密情報
- ❌ `.DS_Store` - macOSのシステムファイル

---

## 🔍 アップロード前の確認

### チェックリスト

```bash
# 1. 現在のディレクトリを確認
pwd
# /Users/kairi.oshima/honma_kaitai/dashboard であることを確認

# 2. フォルダ構造を確認
ls -la

# 3. node_modulesが存在するか確認
ls -d node_modules
# 存在する場合は、アップロード前に除外

# 4. .nextが存在するか確認
ls -d .next
# 存在する場合は、アップロード前に除外

# 5. .env.localが存在するか確認
ls -a | grep .env
# 存在する場合は、アップロード前に除外
```

---

## 📋 推奨アップロード手順

### ステップ1: 準備

```bash
cd /Users/kairi.oshima/honma_kaitai

# 一時的に除外するフォルダを移動
cd dashboard
mv node_modules node_modules_backup 2>/dev/null || true
mv .next .next_backup 2>/dev/null || true
```

### ステップ2: アップロード

1. Finderで `dashboard` フォルダを開く
2. フォルダ全体をドラッグ
3. GitHubのアップロード画面にドロップ
4. コミットメッセージを入力: "Add dashboard application"
5. 「Commit changes」をクリック

### ステップ3: 復元（必要に応じて）

```bash
# 開発を続ける場合は、除外したフォルダを戻す
cd /Users/kairi.oshima/honma_kaitai/dashboard
mv node_modules_backup node_modules 2>/dev/null || true
mv .next_backup .next 2>/dev/null || true
```

---

## 💡 よくある質問

### Q: フォルダをドラッグ&ドロップしても構造が保持されない？

**A**: フォルダ全体をドラッグしているか確認してください。フォルダ内のファイルだけを選択していると、構造が保持されません。

**解決方法**:
- フォルダ名をクリックして、フォルダ全体を選択
- または、Finderのサイドバーからフォルダをドラッグ

### Q: サブフォルダも含めてアップロードされる？

**A**: はい。フォルダをドラッグ&ドロップすると、すべてのサブフォルダとファイルが再帰的にアップロードされます。

### Q: 複数のフォルダを同時にアップロードできる？

**A**: はい。Cmd+クリックで複数のフォルダを選択して、同時にドラッグ&ドロップできます。

### Q: アップロードに時間がかかる？

**A**: ファイル数が多い場合、アップロードに時間がかかることがあります。進捗はブラウザの下部に表示されます。

---

## 🎯 実践例：dashboardフォルダをアップロード

### 1. 準備

```bash
cd /Users/kairi.oshima/honma_kaitai/dashboard

# 除外するフォルダを確認
ls -la | grep -E "node_modules|\.next|\.env"
```

### 2. 除外（必要に応じて）

```bash
# 一時的に除外
mkdir -p ../temp_exclude
mv node_modules ../temp_exclude/ 2>/dev/null || true
mv .next ../temp_exclude/ 2>/dev/null || true
```

### 3. Finderで開く

```bash
# Finderでdashboardフォルダを開く
open .
```

### 4. ドラッグ&ドロップ

1. Finderで `dashboard` フォルダを選択
2. GitHubのアップロード画面にドラッグ&ドロップ
3. コミットメッセージを入力
4. 「Commit changes」をクリック

### 5. 確認

GitHubのリポジトリページで、フォルダ構造が正しくアップロードされているか確認：

- `dashboard/app/` が存在するか
- `dashboard/components/` が存在するか
- `dashboard/lib/` が存在するか
- `node_modules/` が**含まれていない**か

---

## 📚 参考

- [GitHub公式ドキュメント - Uploading files](https://docs.github.com/en/repositories/working-with-files/managing-files/adding-a-file-to-a-repository)
- [GitHub公式ドキュメント - Working with folders](https://docs.github.com/en/repositories/working-with-files/managing-files/working-with-folders)

---

## ⚡ クイックリファレンス

```
1. Finderでフォルダを開く
2. フォルダ全体をドラッグ
3. GitHubのアップロード画面にドロップ
4. コミットメッセージを入力
5. 「Commit changes」をクリック
```

**重要**: `node_modules/`、`.next/`、`.env.local`はアップロード前に除外！

